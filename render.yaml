services:
  - type: web
    name: ai-compression
    runtime: docker
    plan: free
    envVars:
      - key: DB_PATH
        value: /var/data/jobs.db
    disk:
      name: data
      mountPath: /var/data
      sizeGB: 1
